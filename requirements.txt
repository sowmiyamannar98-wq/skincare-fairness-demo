# Runtime dependencies for the demonstrator (app/app.py).
#
# This is what Streamlit Community Cloud installs, so it stays minimal and must
# keep the CPU-only torch pins. For the analysis scripts and notebooks, install
# requirements-analysis.txt as well.
#
# CPU-only torch: the default PyPI wheel bundles CUDA and is several GB, which
# blows the free tier's resource limit. The "+cpu" local version forces the
# small CPU build from the index below. torch is pinned to the version that
# wrote the checkpoint - an older torch may fail to read it, so do not relax
# this without re-testing that armB_acne_resnet50_best.pt still loads.
--extra-index-url https://download.pytorch.org/whl/cpu

torch==2.10.0+cpu
torchvision==0.25.0+cpu

streamlit>=1.40
timm>=1.0.7
pandas>=2.2
pyarrow>=15.0
numpy>=2.0
pillow>=10.0
